# OpenAI API Key (Required)
# Get your API key from https://platform.openai.com/api-keys
OPENAI_API_KEY=your-api-key-here

# Search Context Size (Optional)
# Controls the search context size for advanced search
# Options: low, medium, high (default: medium)
SEARCH_CONTEXT_SIZE=medium

# Reasoning Effort (Optional)
# Controls the reasoning effort level for complex queries
# Options: low, medium, high (default: medium)
REASONING_EFFORT=medium

# API Timeout (Optional)
# API request timeout in seconds (default: 60)
OPENAI_API_TIMEOUT=60

# Max Retries (Optional)
# Maximum number of retries for failed requests (default: 3)
# The client automatically retries on rate limits (429), server errors (5xx), and connection errors
OPENAI_MAX_RETRIES=3

# Default Model (Optional)
# Set the default OpenAI model to use
OPENAI_MODEL=gpt-4o